// Placeholder so the Yahtzee.Presentation assembly compiles before M2 adds MonoBehaviours.
// Presentation classes only render state and forward input; GameController is the sole
// caller of GameEngine mutators (see Docs/TECH_PLAN.md §3).
